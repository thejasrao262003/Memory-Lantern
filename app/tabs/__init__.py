"""Gradio tab builders.

Each module exposes a ``build(...)`` function that constructs the components for
one tab and returns a handle to the components the orchestration layer needs to
wire up callbacks.
"""
