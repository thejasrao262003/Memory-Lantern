"""Pipeline package.

Each module wraps a single Modal inference endpoint. The :mod:`orchestrator`
module strings them together and owns the shared :class:`StoryResult` and
:class:`SceneDict` types.
"""
